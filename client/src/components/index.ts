import { Header, Main, TimelineCard } from './organisms';
import GlobalStyle from './GlobalStyle';

export { Header, Main, TimelineCard, GlobalStyle };
export default { Header, Main, TimelineCard, GlobalStyle };